#' Build a bit flag from an operation that has generated the focal entity
#'
#' @param x [`data.frame(1)`][data.frame]\cr the table that contains...
#' @param test [`character(1)`][character]\cr something
#' @importFrom checkmate assertDataFrame assertCharacter
#' @export

bf_origin <- function(x, test){

  # assertDataFrame(x = x)
  # assertCharacter(x = test, len = 1, any.missing = FALSE)
  #
  # example_data[test] <- rnorm(10)
  #
  # savehistory()
  # hist <- readLines(".Rhistory")
  #
  # return(hist)
}
